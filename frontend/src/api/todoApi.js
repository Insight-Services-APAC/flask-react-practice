// Student needs to implement the API client functions
const API_URL = import.meta.env.VITE_API_URL || '';

export const fetchTodos = async () => {
  // Implement: Fetch all todos from the API
};

export const createTodo = async (todo) => {
  // Implement: Create a new todo via the API
};

export const updateTodo = async (id, todo) => {
  // Implement: Update a todo by ID via the API
};

export const deleteTodo = async (id) => {
  // Implement: Delete a todo by ID via the API
};
