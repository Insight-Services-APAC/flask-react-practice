import React, { useContext } from 'react';
import { TodoContext } from '../../context/TodoContext';

function TodoItem({ todo }) {
  // Implement this component to display a single todo
  // It should show todo details and provide edit/delete functionality
  
  return (
    <li>
      {/* Display todo information and add edit/delete buttons */}
    </li>
  );
}

export default TodoItem;
